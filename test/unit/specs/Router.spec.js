
import Vue from 'vue'
import VueRouter from 'vue-router'

import Router from 'src/router'

describe('Router', () => {

    it('Router should not be null or err', () => {

        should.exist(Router)
    })

})
